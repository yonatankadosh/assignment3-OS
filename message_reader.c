#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/ioctl.h>
#include "message_slot.h"

int main(int argc, char** argv) {
    // Validate arguments
    // Open device file
    // ioctl(MSG_SLOT_CHANNEL)
    // read() message into buffer
    // Print message to stdout
    // Close and exit
}